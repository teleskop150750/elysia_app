{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "kysely": "kysely-codegen --out-file ./src/db/db.d.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "imports": {
    "#root/*": "./src/*"
  },
  "dependencies": {
    "elysia": "latest",
    "kysely": "^0.28.9",
    "kysely-bun-sql": "^0.2.0",
    "pg": "^8.16.3",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "bun-types": "latest",
    "kysely-codegen": "^0.19.0"
  },
  "module": "src/index.js"
}
